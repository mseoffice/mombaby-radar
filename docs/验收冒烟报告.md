# 母婴雷达 AI V1.0 — 验收冒烟报告（#20 e2e-smoke）

> 对齐：附录B 验收标准、设计视角 §4.2 六页验收点。

---

## 1. 自动化集成测试（已通过）

| 测试类 | 覆盖范围 | 状态 |
|---|---|---|
| `AuthIT` | login 返回 token、无 token 访问 content/dashboard 返回 401 | ✅ PASS |
| `ContentFlowIT` | content 状态机 generate→approve→publish→archive + aiGenerated 标识 + 发布锁定 | ✅ PASS |
| `CommentOpsIT` | 评论 ingest→analyze→reply 闭环 + 脱敏 + 回复方案非空 | ✅ PASS |

> 执行：`mvn test`（dev profile，H2 内存数据库，Mock ChatModel），3 个测试类全部通过。

---

## 2. 前端 UI 手测清单

> 见 `docs/e2e-smoke-checklist.md`（6 页面验收点 + 边界/异常场景）。
> V1.0 暂无前端 e2e 框架（Playwright/Cypress），前端自动化 e2e 列为 V1.1 后续项。

---

## 3. 已知风险

| 风险 | 影响 | 对策 |
|---|---|---|
| 真实 DeepSeek 时延未验证 | 内容生成/评论分析 SLA 可能超标 | 待 Key 到位后跑真实时延专项 |
| 前端自动化 e2e 缺失 | UI 回归依赖手工 | V1.1 补 Playwright |
| 容量基线未验证 | 10万商品/50万内容/100万评论容量未知 | 待种子数据灌入 |

---

## 4. 结论

- 核心业务链路（认证 → 内容生成/审核/发布 → 评论分析/回复）已通过自动化集成测试。
- 合规项（AI 标识、审计写入、PII 脱敏）已在 #19 落地并通过测试覆盖。
- 建议 #21 上线前完成前端手测清单（e2e-smoke-checklist.md）的全量签字。
