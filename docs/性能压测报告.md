# 母婴雷达 AI V1.0 — 性能压测报告（#18 perf-baseline）

> 基线来源：PRD 第 34 章（首屏≤2s、API≤200ms、内容生成≤3min、评论分析≤5s、50 并发）。
> 压测工具：k6 v0.51.0（perf/k6/ 下脚本）。

---

## 1. 环境

- **应用**：Spring Boot 3.2.5 单体，dev profile（Mock LLM，无需真实 Key）。
- **基础设施**：docker-compose（MySQL 8 / RabbitMQ 3 / ES 8.13 / MinIO）。
- **HikariCP**：maximum-pool-size=25。
- **ES**：http（xpack 关闭）。
- **Actuator**：已暴露 health/prometheus/metrics。

---

## 2. API 基线（50 VU，30s）

| 端点 | p(95) 目标 | 实测 p(95) | 达标 |
|---|---|---|---|
| POST /api/auth/login | <2s | （待跑） | — |
| GET /api/dashboard | <500ms | （待跑） | — |
| GET /api/products | <200ms | （待跑） | — |
| 失败率 | <1% | （待跑） | — |

> ⚠️ 需启动 docker-compose + 后端后运行：`k6 run --vus 50 --duration 30s perf/k6/api-smoke.js`

---

## 3. 内容生成基线（3 VU，3min）

| 指标 | 目标 | 实测 | 达标 |
|---|---|---|---|
| POST /api/content/generate p(95) | ≤3min | （待跑） | — |
| 失败率 | <10% | （待跑） | — |

> 运行：`k6 run --vus 3 --duration 180s perf/k6/content-generate.js`
> dev Mock profile 下 `ChatClient.call()` 同步返回（<100ms），实际耗时取决于后端编排（AgentTask 创建 + for 循环 + content 保存）。

---

## 4. 评论分析基线（5 VU，30s）

| 指标 | 目标 | 实测 | 达标 |
|---|---|---|---|
| POST /api/comments/{id}/analyze p(95) | ≤5s | （待跑） | — |
| 失败率 | <10% | （待跑） | — |

> 运行：`k6 run --vus 5 --duration 30s perf/k6/comment-analyze.js`

---

## 5. 容量基线（10万商品/50万内容/100万评论）

- **当前状态**：种子数据脚本未运行（待灌入后验证）。
- **方案**：`perf/seed/` 下 SQL 批插或 `CommandLineRunner`。

---

## 6. 真实 LLM 时延专项（待 DeepSeek Key 到位）

- `SPRING_PROFILES_ACTIVE=prod DEEPSEEK_API_KEY=sk-xxx mvn spring-boot:run`
- 重新运行内容生成与评论分析 k6 脚本，验证真实 DeepSeek 时延是否满足 ≤3min/≤5s。

---

## 7. 结论与签字

- 后端集成测试（AuthIT/ContentFlowIT/CommentOpsIT）**已通过**（`mvn test` 0 failures）。
- k6 压测待基础设施就绪（docker-compose up + vm.max_map_count）后运行；真实时延专项待 DeepSeek Key 到位。
- 容量基线待种子数据灌入后验证。

| 角色 | 姓名 | 日期 | 签字 |
|---|---|---|---|
| 技术负责人 | | | |
| 测试负责人 | | | |
