# 母婴雷达 AI V1.0 — 合规检查清单（#19 compliance）

> 对齐：附录C（合规与风险）、35.2（安全/审计）、阶段五测试验收。
> 以下各项需在 V1.0 上线前逐项核查并签字。

---

## 1. AI 生成内容标识（附录C）

| 编号 | 检查项 | 验收标准 | 代码落点 | 状态 |
|---|---|---|---|---|
| C1 | 内容生成时即标注 AI 来源 | `Content.aiGenerated` 在 `ContentGenerateService.generate` 写入时置为 `true`，而非等到 publish | `ContentGenerateService.java:87` | ✅ 已实现 |
| C2 | 发布时再次确认 AI 标识 | `ContentService.publish` 将 `aiGenerated` 置为 `true`（双重保障） | `ContentService.java:71` | ✅ 已实现 |
| C3 | 前端展示「AI 生成」badge | 内容列表中 AI 生成的内容旁显示紫色 badge | `ContentFactory.vue:24` `.ai-badge` | ✅ 已实现 |
| C4 | API 接口返回 AI 标识 | `GET /api/content/{id}/publish-status` 返回 `aiGenerated` 字段 | `ContentController.java:74` | ✅ 已实现 |

---

## 2. 个人信息保护 / 脱敏（附录C / 35.2）

| 编号 | 检查项 | 验收标准 | 代码落点 | 状态 |
|---|---|---|---|---|
| P1 | 评论/私信录入时自动脱敏 | 手机号/身份证号在 `CommentService.save` 写入前自动替换为脱敏占位 | `Desensitizer.comment()` + `CommentService.java:72` | ✅ 已实现 |
| P2 | 手机号脱敏格式 | 前3后4保留，中间4位星号（如 `138****1234`） | `Desensitizer.mobile()` | ✅ 已实现 |
| P3 | 身份证号脱敏格式 | 首1位+末1位保留，中间15位星号 | `Desensitizer.idCard()` | ✅ 已实现 |
| P4 | 前端展示不再含原始 PII | 脱敏在服务端完成，前端仅展示已脱敏文本 | `Desensitizer` 在 save 时调用 | ✅ 已实现 |
| P5 | 昵称/头像脱敏 | V1.0 简化：评论/私信不含昵称/头像字段，仅存平台名+文本 | 设计决策 | ⚠️ 后续版本补齐 |

---

## 3. 操作审计留存（35.2，保留 6 个月）

| 编号 | 检查项 | 验收标准 | 代码落点 | 状态 |
|---|---|---|---|---|
| A1 | 内容审核写入审计日志 | approve/reject 操作自动写入 `operation_log` | `ContentService` + `@AuditLog` | ✅ 已实现 |
| A2 | 内容发布写入审计日志 | publish 操作自动写入 `operation_log` | `ContentService` + `@AuditLog` | ✅ 已实现 |
| A3 | 内容归档写入审计日志 | archive 操作自动写入 `operation_log` | `ContentService` + `@AuditLog` | ✅ 已实现 |
| A4 | 评论分析/回复写入审计日志 | analyze/reply 操作自动写入 `operation_log` | `CommentService` + `@AuditLog` | ✅ 已实现 |
| A5 | 6 个月过期清理 | 每天凌晨 2:00 自动清理 6 个月前的 `operation_log` | `OperationLogCleaner` (`@Scheduled`) | ✅ 已实现 |
| A6 | `content_review_log` 审核追溯 | approve/reject 写入 `content_review_log`（已有） | `ContentService.writeReviewLog` | ✅ 已有 |

---

## 4. 人工审核不可省（假设 A4）

| 编号 | 检查项 | 验收标准 | 代码落点 | 状态 |
|---|---|---|---|---|
| H1 | 内容生成后进入人工审核 | `ContentStatus` 流转：GENERATING → PENDING_REVIEW（不可跳过） | `ContentGenerateService.java:82` | ✅ 已实现 |
| H2 | 待审核状态锁定发布 | `PENDING_REVIEW` 不可发布，前端显示 🔒 | `ContentFactory.vue` + `canPublish` | ✅ 已实现 |

---

## 5. RBAC 最小权限（A7 内部工具可暂缓）

| 编号 | 检查项 | 验收标准 | 状态 |
|---|---|---|---|
| R1 | 角色表已定义 | `role` 实体 + `SysUser` 关联已存在 | ✅ 实体已建 |
| R2 | 接口级别权限校验 | 关键写操作（publish/approve）加 `@PreAuthorize` | ⚠️ V1.1（当前仅 JWT 登录校验，A7 内部工具可暂缓） |

---

## 6. 核查签字

| 角色 | 姓名 | 日期 | 签字 |
|---|---|---|---|
| 产品负责人 | | | |
| 技术负责人 | | | |
| 测试负责人 | | | |
