# 母婴雷达 AI PRD — 后续优化点记录

> 生成日期：2026-07-12（V1.0 PRD 已补完 P0/P1/P2 并回填 D1–D4 之后）
> 说明：经全文扫描（占位标记 + 空壳章节检测 + 章节结构核对），PRD 当前已满足发布基线。本文仅记录**尚未补充、建议后续优化的点**，供进入设计/研发评审阶段前查漏补缺。所有项均**不涉及编造数据**。

## 一、当前已闭环（基线已达标，无需再补）

- **P0/P1/P2 全部补充项**已写入（埋点口径、评审遗留闭环、多租户假设、指标口径、EARS 规范、验收标准总览、数据模型补表、content 状态机、合规增强、个人信息保护、排期风险提示）。
- **决策清单 D1–D4 已回填并上传**：D1 单团队内部工具、D2 接入多平台 API 保证可用（DeepSeek 为主 + 开发 Mock/测试真实 API）、D3 手动粘贴为主 + 平台 API 可选（已闭环）、D4 不指定负责人。
- **结构完整**：38 章无空壳章节；HTML 标签全部平衡；D1（单团队）与假设 A7、竞品对比表口径一致，无多租户矛盾。
- **附录 A/B/C** 分别覆盖埋点口径、验收标准、合规与风险。

## 二、后续优化点清单

| 编号 | 优化点 | 位置 | 优先级 | 建议动作 | 需编造数据 | 状态 |
|---|---|---|---|---|---|---|
| O1 | 指标基线列仅标"待填/—"，缺回填机制 | 1.5 指标口径与定义表（基线列） | 低–中 | 补一句回填机制："立项后 2 周内由运营基于 30 天历史数据回填；净新增指标以 0 为基线"。目标值列已填，无需动 | 否 | 待办 |
| O2 | EARS 规范已定义但未落地到功能需求 | 9.4 需求描述规范 + 第 10–17 章 F-spec | 中（质量） | 将各模块功能需求重写为 EARS 句式（When/If/While/Where + The system shall），使 9.4 规范不悬空 | 否 | 待办 |
| O3 | 示例"购买链接：xxx"占位 | 13.x 内容生成示例 | 低 | 改为"（示例链接，正式版由内容工厂生成）"，避免误读为真实链接 | 否 | 待办 |
| O4 | 评论/私信 V1.0 采集方式 | 附录D D3 / 26.1 评论处理流程 | 已知开放项 | **✅ 已闭环（2026-07-12）**：手动粘贴录入为主 + 平台 API 自动采集为可选项（Collector 抽象预留），回填文档见 PRD 附录D D3 | 否 | 已闭环 |

## 三、跟进建议（与研发节奏对齐）

- **O4（D3）**：✅ 已闭环（2026-07-12）—— 手动粘贴为主 + 平台 API 可选，已回填 PRD 附录D D3，#14 comment-ops 解除 blocked。
- **O2（EARS 落地）**：建议在进入**设计/研发评审（阶段三）**前完成，便于研发按无歧义需求拆解任务、测试据此写用例。
- **O1（指标基线）**：在 V1.0 立项后 2 周内由运营基于历史数据回填；净新增指标可先以 0 为基线。
- **O3（示例占位）**：随下次文档微调一并处理即可，不阻塞任何流程。

## 四、阶段流转提醒

- 当前处于**阶段二（PRD 撰写）收尾**。PRD 已可上传资料库作为统一基线（最新版已覆盖 `FgUiuVpgaFpI`）。
- 进入**阶段三（设计/研发评审）**时，可将 O2、O4 作为评审议题，并把待办拆成事项分配给对应负责人（技术负责人 / 设计师 / 测试同学），附上本 PRD 与本文优化点清单。
- 涉及排期、资源等决策，提醒对应负责人确认；信息不足时不擅自编造结论。
